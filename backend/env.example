# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URLs
# FRONTEND_URL is for admin panel (React app on port 5173)
FRONTEND_URL=http://localhost:5173
# PORTFOLIO_URL is for portfolio website (HTML on port 3000)
PORTFOLIO_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/nahid-admin
# For MongoDB Atlas, use: mongodb+srv://username:password@cluster.mongodb.net/nahid-admin

# JWT Secret Key (Change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production-min-32-characters

# Cloudinary Configuration
# Get these from https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Email Configuration (Gmail)
# For Gmail, you need to:
# 1. Enable 2-Step Verification on your Google Account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use your Gmail address as EMAIL_USER
# 4. Use the generated App Password as EMAIL_PASSWORD
# 5. ADMIN_EMAIL is optional - if not set, EMAIL_USER will be used as admin email
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
# ADMIN_EMAIL=your-email@gmail.com  # Optional: defaults to EMAIL_USER if not set

# bKash Payment Configuration
# Get these from bKash Developer Portal: https://developer.bka.sh
# Using bkash-payment npm package for Tokenized Checkout API integration
#
# Sandbox Configuration (for testing)
BKASH_BASE_URL=https://tokenized.sandbox.bka.sh/v1.2.0-beta
BKASH_APP_KEY=your-bkash-app-key
BKASH_APP_SECRET=your-bkash-app-secret
BKASH_USERNAME=your-bkash-username
BKASH_PASSWORD=your-bkash-password

# Production Configuration (when ready)
# BKASH_BASE_URL=https://tokenized.pay.bka.sh/v1.2.0-beta
# BKASH_APP_KEY=your-production-app-key
# BKASH_APP_SECRET=your-production-app-secret
# BKASH_USERNAME=your-production-username
# BKASH_PASSWORD=your-production-password

# Callback URL (where bKash redirects after payment)
BKASH_CALLBACK_URL=http://localhost:5000/api/payments/bkash/callback

